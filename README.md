Библиотека для работы с gitlab из OneScript.

Пример использования:

```
#Использовать gitlab

Гитлаб.УстановитьСервер("https://сервер.gitlab");
Гитлаб.УстановитьТокен("токен.gitlab");

IDПроекта = 1;
ОткрытыеМР = Гитлаб.ОткрытыеМержРеквесты(IDПроекта);

Для Каждого МР Из ОткрытыеМР Цикл
    Сообщить(СтрШаблон("IID открытого запроса на слияние: %1", МР["iid"]));
КонецЦикла;
```
